"""Streamlit frontend — Phase 1 placeholder page.

This is the minimal service shell only. The real chat UI that posts to the
backend /chat endpoint and renders the agent's structured response is built in
Phase 4; for now this just confirms the frontend container serves a page.
"""

import streamlit as st

st.title("Spec-Driven App")
st.write("Frontend is running. The agent chat UI arrives in a later phase.")
